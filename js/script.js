function init() {
    gameInfo();
}

document.addEventListener('keydown', evt => {
    if (evt.key === 'Escape') {
        
    } else if (evt.key === 'ArrowRight') {
        
    } else if (evt.key === 'ArrowLeft') {
        
    }
});

function gameInfo() {

}